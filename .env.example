PORT=8080
# Get a free key from https://aistudio.google.com/app/apikey
GEMINI_API_KEY=''
# Default for production demo (swap during dev to avoid free-tier limits)
GEMINI_MODEL=models/gemini-2.0-flash-live-001
# Alt dev models:
# GEMINI_MODEL=gemini-live-2.5-flash-preview
# GEMINI_MODEL=gemini-2.0-flash-live-001
# Optional: restrict origins, comma separated. Leave empty to allow all during local dev.
CORS_ORIGINS=http://localhost:5173
